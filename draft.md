trang tổng quan:

- Hoạt động thiết bị trong 7 ngày không hiển thị gì mặc dù có xe hoạt động trong thời gian đó
- "Xu hướng thời gian hoạt động đội xe trong 30 ngày": UI UX quá xuất, khó nhìn và cũng chưa hoạt động
- kiểm tra cả các thành phần khác trong trang này xem tất cả đã khớp và hoạt động thật, không phải placeholder hay chưa, UIUX đã tốt chưa
  sửa các lỗi được liệt kê/không bao gồm sau đây. trong quá trình kiểm tra, khi phát hiện lỗi có thể học hỏi hoặc note để học hỏi, rà soát để sửa các lỗi tương tự ở các trang khác. mục tiêu là hướng đến sản phẩm hoàn thiện đầy đủ tính năng, hoàn thiện uiux tốt, không placeholder, ....

trang quản lý:

- hiển thị tất cả vị trí cuối cùng của các xe (ở đây đang chưa hiển thị). chọn vào thiết bị nào thì focus vào thiết bị - xe đó, nhưng vẫn phải hiển thị đầy đủ tất cả (có thể xem ../anmh1205/IVM26 để học hỏi thêm).
- vùng địa lý: nên rõ ràng, và để vị trí phù hợp hơn (ví dụ cạnh Đường phố/vệ tinh). xử lý lỗi uiux vì khi chọn để tắt thì nó bị chìm do không có nền sau,nghiên cứu xử lý
- ô thông tin thiết bị hiển thị trên phần canvas của map bị trùng thông tin với sub slidebar bên hông, và nó đang chiếm hết view, nên bỏ đi
- kiểm tra cả các thành phần khác trong trang này xem tất cả đã khớp và hoạt động thật, không phải placeholder hay chưa, UIUX đã tốt chưa

trang quản lý/ thiết bị:

- ở bảng thiết bị: nhấn vào hàng của thiết bị để mở modal hiển thị chi tiết lên
- phần filter đang bị xếp thành 2 hàng rất lỗi, kém uiux
- kiểm tra cả các thành phần khác trong trang này xem tất cả đã khớp và hoạt động thật, không phải placeholder hay chưa, UIUX đã tốt chưa

trang quản lý/ Phương tiện:

- kiểm tra cả các thành phần khác trong trang này xem tất cả đã khớp và hoạt động thật, không phải placeholder hay chưa, UIUX đã tốt chưa
- filter đang bị 2 hàng trong khi 1 hàng vẫn trống

trang quản lý/ khách hàng và tài xế:

- kiểm tra cả các thành phần khác trong trang này xem tất cả đã khớp và hoạt động thật, không phải placeholder hay chưa, UIUX đã tốt chưa
- chưa có gán người với xe - theo dõi theo thiết bị từ xe

toàn bộ phần giám sát:

- kiểm tra cả các thành phần khác trong trang này xem tất cả đã khớp và hoạt động thật, không phải placeholder hay chưa, UIUX đã tốt chưa

tương tự với phần Kỹ thuật

- kiểm tra cả các thành phần khác trong trang này xem tất cả đã khớp và hoạt động thật, không phải placeholder hay chưa, UIUX đã tốt chưa

trang phân tích: gặp lỗi tương tự với trang tổng quan

- kiểm tra cả các thành phần khác trong trang này xem tất cả đã khớp và hoạt động thật, không phải placeholder hay chưa, UIUX đã tốt chưa

phần quản trị:

- kiểm tra cả các thành phần khác trong trang này xem tất cả đã khớp và hoạt động thật, không phải placeholder hay chưa, UIUX đã tốt chưa
